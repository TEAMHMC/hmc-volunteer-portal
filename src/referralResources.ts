
import { ReferralResource } from './types';

// Static resources have been moved to the database.
// This array is intentionally empty to ensure the application relies on the API.
export const REFERRAL_RESOURCES: ReferralResource[] = [];
